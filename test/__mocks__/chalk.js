module.exports = {
    redBright: (str) => str,
    greenBright: (str) => str,
    yellow: (str) => str,
    blue: (str) => str,
    cyan: (str) => str,
    magenta: (str) => str,
    white: (str) => str,
    gray: (str) => str,
    black: (str) => str,
    bgRed: (str) => str,
    bgGreen: (str) => str,
    bgYellow: (str) => str,
    bgBlue: (str) => str,
    bgCyan: (str) => str,
    bgMagenta: (str) => str,
    bgWhite: (str) => str,
    bgBlack: (str) => str,
    bold: (str) => str,
    dim: (str) => str,
    italic: (str) => str,
    underline: (str) => str,
    inverse: (str) => str,
    hidden: (str) => str,
    strikethrough: (str) => str,
};